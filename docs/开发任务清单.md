# 开发任务清单

> 项目：基于 LSTM 神经网络的股票价格预测系统  
> 运行基线：Python 3.12  
> 当前状态：**阶段 0 已完成设计，尚未进入阶段 1**  
> 执行方式：单个 Codex Agent 严格串行；每阶段验收通过后再进入下一阶段。

## 0. 阶段 0：需求分析与架构设计

- [x] 阅读《开题报告》并提炼系统目标、用户、范围和约束。
- [x] 定义功能需求与非功能需求。
- [x] 明确核心任务：截至 t 日预测 t+1 真实交易日 close。
- [x] 定义单变量 `close -> next close` 实验。
- [x] 定义多变量 `open/high/low/close/vol/amount -> next close` 实验。
- [x] 设计表现层、业务服务层、数据与模型访问层。
- [x] 设计数据获取、缓存、清洗、样本、训练、回测、实验和可视化模块。
- [x] 设计 Naive、MA5、MA10、MA20、SES、ARIMA、LSTM 统一预测格式。
- [x] 固定 RMSE、MAE、R²、DA、相对 Naive RMSE 提升率定义。
- [x] 明确公共 target_date、公平对比、测试集隔离和最佳验证 checkpoint 规则。
- [x] 设计 Streamlit 页面、显式训练操作和离线答辩流程。
- [x] 设计目录、配置、模型和实验产物结构。
- [x] 创建阶段 0 核心设计文档、论文素材卡和进度记录。
- [x] 人工/脚本交叉检查字段、接口、比例、Python 版本和单 Agent 约束。

**阶段 0 产物**：`docs/需求分析.md`、`docs/系统设计.md`、`docs/数据字典.md`、`docs/实验设计.md`、本清单、`docs/paper/素材卡_阶段0.md`、`docs/progress/阶段 0_架构设计.md`。

**进入阶段 1 条件**：用户验收五份核心设计文档和阶段记录；当前任务不自动进入阶段 1。

## 1. 阶段 1：Python 3.12 环境与项目骨架

- [ ] 创建 README、requirements、`.env.example`、`.gitignore` 和基础配置。
- [ ] 创建 `src/`、`pages/`、`configs/`、`scripts/`、`tests/` 及包结构。
- [ ] 实现配置加载、路径管理、日志与随机种子基础设施。
- [ ] 验证 Python 3.12 依赖可安装，最小导入与测试可运行。
- [ ] 编写阶段 1 测试、文档、素材卡和进度记录。

**验收门**：骨架可导入、配置可校验、无秘密和绝对路径、最小 pytest 通过。

## 2. 阶段 2：Tushare、本地 CSV 与行情缓存

- [ ] 实现标准行情 schema 和字段映射。
- [ ] 实现 Tushare Provider，Token 仅从环境变量读取。
- [ ] 实现本地 CSV Provider 与错误提示。
- [ ] 实现单股票日线缓存、元数据、增量合并和原子写入。
- [ ] 实现网络失败/无 Token 时的本地回退。
- [ ] 使用 mock/固定样例测试，不访问真实网络。
- [ ] 编写数据源说明、素材卡和阶段记录。

**验收门**：标准字段正确、缓存可复用、重复日期不残留、离线 CSV 可加载。

## 3. 阶段 3：数据清洗、时间划分与滑动窗口

- [ ] 实现字段类型、升序、去重、缺失和 OHLC/成交字段校验。
- [ ] 实现前向填充且不使用未来值，输出 DataQualityReport。
- [ ] 实现日期缺口识别、样本丢弃和原因记录。
- [ ] 实现 70/15/15 顺序划分，无 shuffle、目标日期无重叠。
- [ ] 实现训练集 MinMaxScaler 与 scaler 元数据。
- [ ] 实现单变量/多变量 60 日滑动窗口和跨段历史上下文。
- [ ] 测试 `input_end_date < target_date`、特征顺序和边界样本。
- [ ] 编写 `docs/数据处理说明.md`，说明选择 60 日窗口的理由。
- [ ] 编写素材卡和阶段记录。

**验收门**：排序、划分、缩放、窗口和缺口规则均有自动测试证明无泄漏。

## 4. 阶段 4：Naive / MA / SES / ARIMA 基线模型

- [ ] 实现统一 baseline 接口和 PredictionFrame schema。
- [ ] 实现 Naive、MA5、MA10、MA20。
- [ ] 实现 SES 一步滚动预测。
- [ ] 实现 ARIMA 配置、拟合和一步滚动预测。
- [ ] 确保每个预测只使用 forecast_origin 及以前数据。
- [ ] 测试短序列、缺口、拟合失败和输出日期。
- [ ] 编写基线说明、素材卡和阶段记录。

**验收门**：六种基线均输出统一格式，previous_close 与 target_date 可复核。

## 5. 阶段 5：PyTorch LSTM 训练、预测与持久化

- [ ] 实现单变量与多变量 LSTM 网络。
- [ ] 实现默认训练参数、MSELoss、Adam、设备选择和固定种子。
- [ ] 实现 train/val 循环、patience=10 早停和最低 val_loss checkpoint。
- [ ] 实现反归一化预测与 PredictionFrame 输出。
- [ ] 保存 state_dict、scaler、模型元数据和训练历史。
- [ ] 加载时校验特征顺序、窗口、架构、数据和版本元数据。
- [ ] 测试最佳 epoch 保存、测试集隔离、持久化往返和异常数值。
- [ ] 编写模型说明、素材卡和阶段记录。

**验收门**：可使用固定小样例完成训练/加载/预测闭环，且保存的是验证最优模型。

## 6. 阶段 6：评价指标、回测与实验管理

- [ ] 实现按 `ts_code + target_date + split` 的交集对齐。
- [ ] 实现 RMSE、MAE、R²、DA 三分类和相对提升率。
- [ ] 实现一步 walk-forward 回测编排。
- [ ] 实现主对比组与 LSTM 消融组隔离。
- [ ] 实现 experiment_id、manifest、状态、配置快照和产物清单。
- [ ] 保存 metrics、predictions、alignment、history、耗时、种子和失败信息。
- [ ] 测试日期不齐、naive_rmse=0、平盘、R²不可定义等边界。
- [ ] 编写评价/实验说明、素材卡和阶段记录。

**验收门**：每项指标可由逐日预测复算，所有模型使用同一公共日期，实验可完整追溯。

## 7. 阶段 7：Streamlit 可视化系统

- [ ] 实现首页/系统说明与投资风险声明。
- [ ] 实现数据管理和质量报告页。
- [ ] 实现实验配置页；训练只由显式按钮触发。
- [ ] 实现预测与回测页、模型对比页、实验记录页。
- [ ] 页面只调用 service 层，不复制算法。
- [ ] 实现离线状态、数据截止日期和产物下载展示。
- [ ] 测试页面导入、service 调用和刷新不自动训练。
- [ ] 编写使用说明、素材卡和阶段记录。

**验收门**：本地可启动，主要流程可操作，页面无网络/训练副作用。

## 8. 阶段 8：正式实验与论文图表导出

- [ ] 冻结正式真实行情数据、配置和数据指纹。
- [ ] 执行单变量主对比实验。
- [ ] 执行单变量/多变量 LSTM 特征消融。
- [ ] 按 20/30/60、32/64/128、1/2、0.01/0.001/0.0001 做控制变量实验。
- [ ] 不使用测试集选择参数，最终配置冻结后执行测试。
- [ ] 导出可复现表格、预测明细、对齐报告和论文图表。
- [ ] 如数据/Token/算力不足，只保存脚本与配置并报告阻塞，不伪造结果。
- [ ] 编写实验结果说明、素材卡和阶段记录。

**验收门**：正式结果均有 experiment_id、真实数据、完整产物和无泄漏证据。

## 9. 阶段 9：全面测试、数据泄漏审查与答辩交付

- [ ] 执行最小单测、模块测试、集成测试和可行时的全量 pytest。
- [ ] 逐项审查排序、划分、scaler、窗口、早停、公共日期和公平比较。
- [ ] 验证无 Token 的本地 CSV 离线完整流程。
- [ ] 验证预训练模型与预生成实验的离线答辩流程。
- [ ] 完成 README、安装使用说明、测试报告、交付清单和答辩演示步骤。
- [ ] 检查无秘密、绝对用户路径、固定盘符和伪造结果。
- [ ] 编写最终素材卡和阶段记录。

**验收门**：Python 3.12 可安装启动，测试通过，离线演示完整，结果可追溯，无已知严重泄漏。

## 全程不可违反项

- [ ] 不创建或委派子 Agent，不并行修改文件。
- [ ] 不执行 `git commit`、`git reset` 或破坏性删除。
- [ ] 不在测试中访问真实网络。
- [ ] 不硬编码 Token、密码、用户绝对路径或固定盘符。
- [ ] 不用测试集调参，不保存最后 epoch 冒充最佳模型。
- [ ] 不将多变量 LSTM 混入传统方法主对比表。
- [ ] 不伪造行情、指标、训练记录或实验结论。
