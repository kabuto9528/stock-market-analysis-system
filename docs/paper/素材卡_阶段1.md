# 素材卡：阶段 1 Python 3.12 环境与项目骨架

## 基本信息

- 阶段：1
- 日期：2026-09-15
- 状态：已完成，等待验收
- 依据：阶段 0 架构设计与《开发任务清单》

## 可用于论文的工程结论

1. 项目采用 Python 3.12，使用配置驱动和项目相对路径，避免 Token、用户路径和固定盘符硬编码。
2. 工程按表现层、服务层、领域算法和数据/模型访问职责预留目录，便于后续测试和替换数据源。
3. 默认配置固定 70%/15%/15% 的时间顺序划分、60 日滑动窗口、LSTM 超参数和随机种子，为后续实验复现提供统一起点。
4. 配置加载阶段即校验比例、正数超参数和路径越界，降低实验配置错误与数据目录误写风险。

## 可写入论文章节

- 系统实现环境：Python 3.12、PyTorch、Streamlit、Pandas 等。
- 系统总体设计：配置驱动、分层目录和可复现运行基础设施。
- 系统部署与运行：虚拟环境、依赖安装、环境变量和离线启动方式。

## 预定图表素材

- 项目目录与分层架构图（后续结合各阶段实际模块补充）。
- 配置文件参数表（后续实验阶段补充最终配置快照）。

## 答辩口径

- 阶段 1 只完成可运行骨架和基础设施，没有用合成数据冒充行情，也没有提前实现训练。
- 首页刷新不会自动访问网络或启动训练；真实数据和模型功能按阶段顺序实现。
- R²、DA 和相对 Naive 提升率等评价口径来自阶段 0，阶段 1 不产生模型结论。

## 后续待补证据

- 阶段 2 补充本地 CSV、Tushare mock、缓存和数据质量报告证据。
- 阶段 3 补充无泄漏划分、训练集 scaler 和窗口边界测试证据。
