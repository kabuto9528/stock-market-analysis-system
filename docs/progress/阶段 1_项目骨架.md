# 阶段 1：Python 3.12 环境与项目骨架

- 日期：2026-09-15
- 状态：完成，等待用户验收；未进入阶段 2
- 执行方式：单 Agent 串行，未创建或委派子 Agent

## 完成内容

1. 创建 Streamlit 入口 `app.py` 及 `pages/`、`src/` 子包、`configs/`、`scripts/`、`tests/`、`data/`、`artifacts/` 目录。
2. 为项目包和测试包补充 `__init__.py`，为运行目录补充 `.gitkeep` 占位文件。
3. 创建 Python 3.12 依赖清单，覆盖 PyTorch、Streamlit、Pandas、NumPy、Scikit-learn、Statsmodels、Plotly、Tushare、PyYAML、python-dotenv、Joblib 和 Pytest。
4. 创建不含真实密钥的 `.env.example`，并完善 `.gitignore`，保留 `data/.gitkeep` 与 `artifacts/.gitkeep`。
5. 创建 `configs/default.yaml`：70/15/15 时间比例、60 日窗口、LSTM 默认参数、随机种子和项目相对存储目录。
6. 实现 `src/config.py`：YAML 与环境变量读取、比例/超参数校验、项目内相对路径解析和运行目录创建。
7. 实现 `src/runtime.py`：日志基础设施和 Python、NumPy、PyTorch、CUDA 随机种子设置。
8. 创建最小首页：项目名称、使用流程、当前开发状态和“不构成投资建议”声明；未实现数据获取和训练。
9. 创建 README，说明 Python 3.12 虚拟环境、依赖安装、Token 配置、测试、导入检查和 Streamlit 启动。
10. 创建配置、路径、模块导入和随机种子测试。

## 验证结果

- Python：3.12.6
- 依赖安装：`.venv` 内成功安装 `requirements.txt`
- 配置测试：`python -m pytest tests/test_config.py -q` → 4 passed
- 全量当前测试：`python -m pytest -q` → 16 passed
- 最小导入：项目模块及全部阶段 1 依赖导入检查通过

## 范围控制

- 未实现真实行情获取、Tushare 调用、数据清洗、基线模型、LSTM 训练和实验评价。
- 未写入真实 Tushare Token、用户绝对路径或固定盘符配置。

## 下一阶段条件

用户验收阶段 1 文件、测试和文档后，方可进入阶段 2：Tushare、本地 CSV 与行情缓存。
