# AGENTS.md

本文件是项目级开发规范。每次任务开始前自动生效，**不需要再向 Agent 重复粘贴通用规则**。

---

## 一、项目

- **名称**：基于LSTM神经网络的股票价格预测系统
- **目录**：项目仓库根目录（禁止在代码或配置中硬编码本机绝对路径）
- **依据**：`开题报告.md`（需求与范围的唯一来源）
- **技术栈**：Python 3.12 / PyTorch / Streamlit / Pandas / NumPy / Scikit-learn / Statsmodels / Plotly / Tushare / PyYAML / python-dotenv / Joblib / Pytest
- **执行主体**：单个 Codex Agent，严格串行，不创建或委派任何子 Agent，不并行修改文件

---

## 二、阶段顺序（严格串行，验收通过才可进入下一阶段）

```
0  需求分析与架构设计
1  Python 3.12 环境与项目骨架
2  Tushare、本地 CSV 与行情缓存
3  数据清洗、时间划分与滑动窗口
4  Naive / MA / SES / ARIMA 基线模型
5  PyTorch LSTM 训练、预测与持久化
6  评价指标、回测与实验管理
7  Streamlit 可视化系统
8  正式实验与论文图表导出
9  全面测试、数据泄漏审查与答辩交付
```

阶段任务提示词见 `Codex Agent分阶段开发方案.md`。

---

## 三、每阶段闭环

1. 阅读 `开题报告.md`、README、`docs/`、上一阶段记录与相关代码
2. 只实现当前阶段，不提前实现后续阶段的大型功能
3. 配套测试与文档
4. 实际运行测试（不跑通不算完成）
5. **写 `docs/paper/素材卡_阶段N.md`**（格式见 `论文轨方案.md`）
6. 写 `docs/progress/阶段N_*.md` 阶段记录
7. 按验收标准自检，然后汇报：修改文件、实现功能、测试命令与结果、遗留问题、下一阶段条件

---

## 四、硬规则

### 数据与实验正确性

1. 行情按 `trade_date` 升序排列，删除重复日期
2. 时间序列按 70 / 15 / 15 **顺序**划分，禁止 shuffle；三组目标日期不得重叠
3. MinMaxScaler **只在训练集 fit**，验证集与测试集只允许 transform
4. 验证集用于调参和早停；**测试集只用于最终评价**，不得参与任何选择
5. 保存**验证损失最低**的模型，不得用最后一个 epoch 冒充最优模型
6. 滑动窗口的目标日期必须严格晚于输入窗口，禁止未来信息进入特征
7. 验证/测试集的首个输入窗口可使用前一区间末尾的历史数据，但**目标日期必须属于当前区间**
8. **停牌与日期缺口**：识别连续缺口，保证 Naive/MA 所依据的"前一交易日"是真实交易日；无法保证时丢弃该样本并记录
9. 所有模型必须在**相同 target_date 集合**上比较；日期集合不一致时取公共日期并显式记录
10. **与传统方法对比的公平性**：Naive / MA / SES / ARIMA 为单变量模型，**只能与「单变量 LSTM（仅 close 输入）」对比**；多变量 LSTM 归入特征消融实验单独讨论，不得混入同一张主对比表
11. 固定随机种子（默认 42），同时设置 Python / NumPy / PyTorch / CUDA 种子
12. 每次实验保存完整配置、指标、逐日预测、耗时与种子，可通过 experiment_id 完整追溯

### 评价指标

13. 实现 RMSE、MAE、R²、涨跌方向准确率 DA、相对 Naive 的 RMSE 提升率
14. **DA 定义**：预测方向比较 `predicted` 与 `previous_close`，实际方向比较 `actual` 与 `previous_close`；平盘（涨跌幅 = 0）按三分类符号一致规则处理，并在 docstring 与文档中写死
15. 相对提升率 = `(naive_rmse − model_rmse) / naive_rmse × 100%`
16. **R² 仅作价格水平拟合度的参考，不得用于模型排名**；模型优劣依据 DA 与相对提升率。禁止出现"R² 很高所以精度高"这类表述

### 工程约束

17. 不硬编码 Tushare Token、密码、用户绝对路径或固定盘符；配置统一走 `configs/*.yaml` 与环境变量
18. 不伪造行情数据、模型指标、训练记录或实验结论；数据不足时**只生成实验配置与运行脚本并报告阻塞原因**，不得用合成数据冒充正式结果
19. 不覆盖已有正确功能，不删除用户文件
20. **不执行 `git commit` / `git reset` / 任何破坏性删除**（版本控制由用户手动进行）
21. 测试不得访问真实网络；Tushare 相关测试一律使用 mock 或固定样例
22. Streamlit 页面只调用 service 层，不在页面内重复实现数据处理、训练或评价算法；**页面刷新不得自动触发训练**

### 输出纪律（降低 token 消耗，必须遵守）

23. **不做全仓库搜索**。任务描述中已列出相关文件，直接读指定文件；确实需要查找时先用 Glob 定位，不逐文件通读
24. **不把文件内容整段复述到对话里**，直接写入目标文件
25. 代码写入文件，**不在对话中输出长代码块**（超过 20 行就必须落盘）
26. 测试优先跑最小范围（`pytest <file>::<case> -q`），不无差别跑全量测试
27. 汇报限于三段：**文件路径清单、关键决策或结果、遗留问题**，合计不超过 30 行
28. 阶段收尾写进度记录时**控制在 100 行以内，只写结论不写过程**，供下一阶段会话直接读取
29. 不重复读取同一文件；不询问已经写在 `AGENTS.md` 或任务描述中的信息

---

## 五、默认参数

| 参数 | 默认值 |
| --- | ---: |
| 训练 / 验证 / 测试比例 | 70% / 15% / 15% |
| 滑动窗口 | 60 个交易日 |
| hidden_size | 64 |
| num_layers | 2 |
| dropout | 0.2 |
| batch_size | 32 |
| learning_rate | 0.001 |
| max_epoch | 100 |
| early_stopping_patience | 10 |
| 损失函数 | MSELoss |
| 优化器 | Adam |
| 随机种子 | 42 |

**参数实验取值（阶段 8，控制变量，禁止无边界搜索）**

- 窗口：20 / 30 / 60
- hidden_size：32 / 64 / 128
- num_layers：1 / 2
- learning_rate：0.01 / 0.001 / 0.0001

> 默认窗口 60 个交易日（约 3 个月）需在 `docs/数据处理说明.md` 中写明选择理由——答辩会问"为什么是 60"，必须能答。

---

## 六、数据规范

**标准字段**：`ts_code`、`trade_date`、`open`、`high`、`low`、`close`、`vol`、`amount`

- 单变量实验输入：`close`
- 多变量实验输入：`open`、`high`、`low`、`close`、`vol`、`amount`
- **预测任务**：使用截至第 t 日的数据，预测第 t+1 交易日的收盘价

---

## 七、项目目录

```
app.py                  Streamlit 入口
pages/                  多页面
src/data/               数据获取、缓存、清洗
src/models/             LSTM 模型与训练器
src/baselines/          Naive / MA / SES / ARIMA
src/evaluation/         指标与回测
src/services/           业务服务层
src/visualization/      图表
configs/                YAML 配置
scripts/                命令行工具
tests/                  Pytest
data/                   行情缓存（不入版本库）
artifacts/              模型与实验结果（不入版本库）
docs/                   设计文档与阶段记录
docs/paper/             论文素材卡
docs/progress/          阶段执行记录
```
