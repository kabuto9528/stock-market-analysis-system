# 当前进度

> 新会话先阅读本文件、`AGENTS.md`、上一阶段记录和当前阶段相关代码。

## 项目

- 名称：基于 LSTM 神经网络的股票价格预测系统
- 目录：`D:\1graduation_project`
- 当前日期：2026-09-16

## 阶段状态

| 阶段 | 状态 | 说明 |
| --- | :-: | --- |
| 0-5 | 已完成 | 架构、骨架、数据、预处理、基线与 LSTM |
| 6 | 已完成 | 评价指标、统一测试集回测与实验管理 |
| 7-9 | 未开始 | 必须继续按顺序执行 |

## 阶段 6 摘要

- 已实现 RMSE、MAE、R²、三分类 DA 和相对 Naive RMSE 提升率，并统一校验长度、日期、NaN、Infinity 和最少样本数。
- `UnifiedTestBacktestService` 只接受测试目标日期；模型日期不一致时先取交集，保存原始、缺失、排除与公共日期。
- 比较表按 DA、相对提升率和 RMSE 排序；R² 只作价格水平拟合度参考。
- `ExperimentStore` 保存配置、指标、逐日预测、模型比较、对齐报告和完整实验记录，可由 experiment_id 追溯。
- `scripts/run_experiment.py` 一次运行六个单变量基线与固定单变量 LSTM，测试集只评价一次。
- 固定小型样例明确标记为测试结果，未形成或伪造正式实验结论。
- 详细结论见 `docs/progress/阶段 6_评价回测.md` 和 `docs/评价与回测说明.md`。

## 下一步

1. 阶段 6 已验收通过，下一阶段为阶段 7：Streamlit 可视化系统。
2. 页面必须只调用 service 层，不得在页面内重复实现数据、训练或评价算法。
3. 页面刷新不得自动训练；正式实验与论文图表仍留待阶段 8。
4. 版本控制由用户手动执行；Agent 不执行 commit/reset。
