# 当前进度

> 新会话先阅读本文件、`AGENTS.md`、上一阶段记录和当前阶段相关代码。

## 项目

- 名称：基于 LSTM 神经网络的股票价格预测系统
- 目录：`D:\1graduation_project`
- 当前日期：2026-09-16

## 阶段状态

| 阶段 | 状态 | 说明 |
| --- | :-: | --- |
| 0 | 已完成 | 需求分析与架构设计 |
| 1 | 已完成 | Python 3.12 环境与项目骨架 |
| 2 | 已完成 | Tushare、本地 CSV 与行情缓存 |
| 3 | 已完成 | 数据清洗、时间划分与滑动窗口 |
| 4 | 已完成 | Naive / MA / SES / ARIMA 基线模型 |
| 5 | 已完成 | PyTorch LSTM 训练、预测与持久化 |
| 6-9 | 未开始 | 必须继续按顺序执行 |

## 阶段 5 摘要

- 单变量和多变量共用 `LSTMRegressor`，采用 `batch_first=True`，输出一个下一交易日收盘价。
- 已实现 Python、NumPy、PyTorch、CUDA 随机种子与 CPU/CUDA 自动设备选择。
- 训练器固定使用 MSELoss、Adam，仅以验证损失 Early Stopping，并恢复最佳 epoch 权重。
- 模型与 Scaler 分开保存；加载时校验特征数量、顺序、窗口长度和模型结构参数。
- 已实现带日期的批量预测及显式目标日期的下一交易日预测。
- `scripts/train_model.py` 可从本地 CSV 训练单变量或多变量模型，不使用测试集调参或早停。
- 详细结论见 `docs/progress/阶段 5_LSTM模型.md` 和 `docs/LSTM模型说明.md`。

## 下一步

1. 阶段 5 已验收通过，下一阶段为阶段 6：评价指标、回测与实验管理。
2. 阶段 6 必须让单变量 LSTM 与传统单变量基线在相同 target_date 集合上比较。
3. 测试集只允许用于最终评价；R² 不用于模型排名。
4. 版本控制由用户手动执行；Agent 不执行 commit/reset。
