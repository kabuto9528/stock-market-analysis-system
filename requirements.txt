# ⚠️ torch 必须先单独安装 GPU 版本，否则 pip 会拉取 CPU 版（训练慢 10-50 倍）
#   1) python -m pip install torch --index-url https://download.pytorch.org/whl/cu126
#   2) python -m pip install -r requirements.txt
#   3) 验证：python -c "import torch; print(torch.cuda.is_available())"  -> 应为 True
torch>=2.2.0,<3.0
streamlit>=1.32.0,<2.0
pandas>=2.2.0,<3.0
numpy>=1.26.4,<3.0
scikit-learn>=1.4.0,<2.0
statsmodels>=0.14.1,<1.0
plotly>=5.19.0,<7.0
tushare>=1.4.0,<2.0
PyYAML>=6.0.1,<7.0
python-dotenv>=1.0.1,<2.0
joblib>=1.3.2,<2.0
pytest>=8.0.0,<10.0
