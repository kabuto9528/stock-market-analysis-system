# 开发手册

> 项目：基于LSTM神经网络的股票价格预测系统 ｜ 目录 `D:\1graduation_project`
>
> 本文档分两部分：
> - **第一部分 · 操作指南** —— 怎么跑 Codex、何时换会话、怎么省 token。**先看这个。**
> - **第二部分 · 阶段任务** —— 阶段 0 至 9 的提示词与验收标准。每阶段复制对应那一段给 Codex。
>
> 开发规范已写进 `AGENTS.md`，Codex 自动读取，**不需要粘贴**。

---

# 第一部分 · 操作指南

## 一、启动

```bash
cd /d D:\1graduation_project
codex -a auto-edit
```

**根目录的 `AGENTS.md` 会被自动读取**，所以不需要再粘贴通用规则 —— 每次只贴阶段提示词。

---

## 二、三种审批模式

| 模式 | 命令 | 自动改文件 | 自动跑命令 | 用在哪 |
| --- | --- | :-: | :-: | --- |
| `suggest` | `codex` | ✗ | ✗ | 想先看它打算怎么做时 |
| **`auto-edit`** | `codex -a auto-edit` | ✓ | ✗ | **阶段 0–8 主力** |
| `full-auto` | `codex -a full-auto` | ✓ | ✓ | **只用于阶段 9 跑测试** |

交互中可用 `/approval` 随时切换。

> ⚠️ **阶段 1–8 不要用 `full-auto`**。它会在不询问的情况下执行命令，包括删除类操作。方案里「不许破坏性删除」是提示词约束而非技术约束，别把安全寄托在 AI 听话上。阶段 9 全是 `pytest`，用 full-auto 无风险。

---

## 三、每个阶段的标准动作

```
启动 Codex（auto-edit）
   ↓
开场：读上一阶段进度记录 → 执行阶段 N
   ↓
执行（途中该确认的命令确认）
   ↓
收尾：让它写进度记录 + 素材卡 + 简报
   ↓
你人工验收（对照原方案的验收标准）
   ↓
验收通过 → 你手动 git commit
   ↓
关掉会话，下一阶段开新会话
```

---

## 四、开场提示词模板

```
读 docs/progress/<上一阶段文件名>.md，然后执行阶段 N。
```

阶段 0 没有上一阶段，直接说「执行阶段 0」。

---

## 五、收尾提示词模板（每阶段结束必发）

```
本阶段完成。请依次完成：

1. 写 docs/progress/阶段N_xxx.md，包含：阶段目标、实际完成、修改文件清单、
   接口变化、测试命令与结果、遗留问题、下一阶段前置条件。
   控制在 100 行以内，只写结论不写过程。

2. 写 docs/paper/素材卡_阶段N.md，按《论文轨方案.md》3.3 的模板。

3. 汇报：新增文件、修改文件、测试结果、未完成事项。不超过 30 行。

不要复述已经写进文件里的内容。
```

「**只写结论不写过程**」和「**不要复述**」这两句是省 token 的关键 —— 下一会话读摘要比重放历史便宜得多。

---

## 六、什么时候换会话

**该换：**

| 信号 | 说明 |
| --- | --- |
| 阶段验收通过 | 最标准的换点，无脑换 |
| Agent 开始忘事 | 重复问已说过的信息、违反 AGENTS.md 规则 |
| 上下文进度条快满 | Codex 会提示 |
| 一轮跑了 20+ 次工具调用 | 上下文已经很重 |
| 要切到不相关的大任务 | 别把两件事塞一个会话 |

**不该换：** 任务做到一半、正在调一个具体 bug。

> **省 token ≠ 把多个阶段塞进一个会话。** 上下文一满，Agent 就开始违反规则、重复读文件、忘记约定，返工成本远超省下的钱。该换就换。

---

## 七、省 token 的十条

**结构性（收益最大）**

1. 一阶段一会话，阶段边界即经济边界
2. 规则写进 `AGENTS.md`，不重复粘贴
3. 用进度记录交接，不重放历史对话
4. 说文件路径，不贴文件内容 —— 「改 `src/config.py` 的 `load_config`」
5. 明确禁止全库探索 —— 「不要做全仓库搜索，相关文件已在上下文中列出」

**操作层**

6. 不要让它复述 —— 别问「你刚才做了什么」
7. 代码写进文件，不打在对话里
8. 测试用最小样例 —— `pytest tests/test_x.py::test_y -q` 优于 `pytest -v`
9. 一次只做一件事 —— 别说「顺便把 X 也改一下」
10. 长输出落盘，只汇报路径

**模型层**

- 阶段 0、4、5（需要设计判断）→ 强模型
- 阶段 1、2、7（照提示词写代码）→ 便宜模型，`codex --model <model>`

**版本控制**

- 每阶段验收后**你手动** `git commit`。这既让 Agent 不必「读代码了解现状」，也避免它改坏后产生大段调试轮次

---

## 八、阶段-会话映射

| 阶段 | 新会话 | 模式 | 备注 |
| --- | :-: | --- | --- |
| 0 需求与架构 | ✓ | auto-edit | 纯文档，最省 |
| 1 项目骨架 | ✓ | auto-edit | |
| 2 数据模块 | ✓ | auto-edit | 测试 mock，不联网 |
| 3 数据预处理 | ✓ | auto-edit | 防泄漏测试是重点 |
| 4 基线模型 | ✓ | auto-edit | |
| 5 LSTM | ✓ | auto-edit | 训练会跑命令 |
| 6 评价与回测 | ✓ | auto-edit | |
| 7 Streamlit | ✓ | auto-edit | |
| 8 正式实验 | ✓ | auto-edit | 耗时长，后台跑 |
| 9 全面测试 | ✓ | **full-auto** | 全是 pytest，安全 |

---

## 九、阶段验收提示词（每个阶段跑完后用）

**不要问 Codex「你做得怎么样」** —— 开发方评自己会自我合理化。开一个**独立会话**来验收。

**为什么必须独立窗口**：同一上下文里，Codex 记得自己"打算怎么做"，于是倾向于认为已经做到了。新窗口只看**结果文件**，检查的是"实际写成了什么"，这才是验收该有的视角。

~~~text
我要验收一个毕业设计项目的阶段 N 产出。

项目目录：D:\1graduation_project
判定依据：AGENTS.md 的硬规则 + 开发手册.md 中「阶段 N」的验收标准

只做检查，不要修改任何文件。按顺序：

1. 读 AGENTS.md 与 开发手册.md 中阶段 N 的验收标准
2. 读 docs/progress/阶段N_*.md 及本阶段产出的全部文件
3. 逐条比对验收标准，分三类输出：
   - 通过项
   - 未通过项（写清：标准是什么、实际是什么、差在哪）
   - 无法判定项（说明缺什么信息）
4. 额外专项检查：
   - 数据泄漏：Scaler 是否只在训练集 fit、是否 shuffle、三组目标日期是否重叠
   - 与传统方法对比是否锁定单变量
   - 字段名 / 接口名 / 术语在跨文档之间是否一致
   - 有无 [[待补]]、TODO、pass 占位残留
   - 有无硬编码 Token、绝对路径、固定盘符
5. 给出结论：通过 / 有条件通过（列出必须补的项）/ 不通过

不要复述文件内容，不要修改文件，报告控制在 60 行以内。
~~~

**验收不通过怎么办**：把验收报告贴**回 Codex 那个开发会话**（不要在验收窗口里改），让它针对"未通过项"逐条修复。修完再开新窗口复验。

---

# 第二部分 · 阶段任务

> **怎么用**：每个阶段**只复制「可复制提示词」代码块里的内容**发给 Codex。
>
> 代码块外的「阶段目标 / 前置条件 / 预期输出 / 测试要求 / 验收标准 / 下一阶段进入条件」都是**给你自己看的** —— 阶段跑完后对照着验收用。发给 Codex 只是白花 token。
>
> 唯一例外：想让它自检更严格时，可在提示词末尾追加一句：
> 「完成后逐条对照本阶段的验收标准自检，未达标项直接修复，不要留到下一阶段。」

---

# 阶段 0：需求分析与架构设计

## 阶段目标

把开题报告转化为可实施的软件需求、系统架构、数据规范、实验规范和开发任务清单。

## 前置条件

- 项目根目录存在开题报告.md。
- 尚未开始核心业务实现。

## 可复制提示词

~~~text
现在执行阶段 0：需求分析与架构设计。

本阶段只完成分析和设计文档，不实现模型训练和Streamlit业务页面。

请完成：
1. 从开题报告提炼功能需求和非功能需求。
2. 将系统划分为表现层、业务服务层、数据与模型访问层。
3. 设计数据获取、缓存、清洗、样本构造、模型训练、回测评价、实验管理和可视化模块。
4. 明确核心预测任务为使用截至第t日的数据预测第t+1交易日收盘价。
5. 设计单变量实验：输入close，目标为下一交易日close。
6. 设计多变量实验：输入open、high、low、close、vol、amount，目标为下一交易日close。
7. 设计Naive、MA5、MA10、MA20、SES、ARIMA与LSTM的统一预测结果格式。
8. 设计RMSE、MAE、R²、DA和相对Naive RMSE提升率。
9. 设计Streamlit页面和用户操作流程。
10. 设计项目目录、配置方式、模型文件和实验结果存储方式。
11. 明确数据泄漏防护规则、异常场景和离线答辩方案。
12. 创建docs/需求分析.md、docs/系统设计.md、docs/数据字典.md、docs/实验设计.md、docs/开发任务清单.md。
13. 创建docs/progress/阶段 0_架构设计.md并记录本阶段完成情况。

完成后检查各文档中的字段、接口和实验规则是否一致。不要创建子Agent，不要进入阶段 1。
~~~

## 预期输出

- 需求分析、系统设计、数据字典、实验设计和开发清单。
- 确定项目分层、数据字段、模型输入输出和页面结构。

## 测试要求

- 人工交叉检查文档中的字段和比例。
- 检查所有文档均指定 Python 3.12。
- 检查无多 Agent 或并行执行内容。

## 验收标准

- 需求与开题报告一致。
- 系统边界明确，不包含自动交易、新闻情感分析等首版范围外功能。
- 数据、模型、回测和页面接口无冲突。

## 下一阶段进入条件

五份核心设计文档和阶段记录均已完成，开发任务清单可直接指导项目初始化。

---

# 阶段 1：Python 3.12 环境与项目骨架

## 阶段目标

创建可安装、可测试、可启动的项目骨架和基础配置。

## 前置条件

阶段 0 验收通过。

## 可复制提示词

~~~text
现在执行阶段 1：Python 3.12环境和项目骨架。

请严格依据阶段 0文档完成：
1. 创建app.py以及pages、src/data、src/models、src/baselines、src/evaluation、src/services、src/visualization、configs、scripts、tests、data和artifacts目录。
2. 创建所有必要的__init__.py。
3. 创建requirements.txt，依赖适配Python 3.12，包含PyTorch、Streamlit、Pandas、NumPy、Scikit-learn、Statsmodels、Plotly、Tushare、PyYAML、python-dotenv、Joblib和Pytest。
4. 创建.env.example，只提供TUSHARE_TOKEN占位符，不写真实Token。
5. 创建.gitignore，忽略虚拟环境、环境变量、缓存、模型、数据和临时实验产物，但保留必要的目录占位文件。
6. 创建configs/default.yaml，写入数据集比例、LSTM默认参数、存储目录和随机种子。
7. 实现src/config.py：读取YAML和环境变量、校验比例、解析项目相对路径、创建运行目录。
8. 创建最小Streamlit首页，展示项目名称、使用流程、当前开发状态和“不构成投资建议”。
9. 创建README.md，说明Python 3.12虚拟环境、依赖安装、环境变量、测试和启动命令。
10. 添加配置读取、路径解析和模块导入测试。
11. 运行pytest和最小导入检查。
12. 更新docs/开发任务清单.md并创建docs/progress/阶段 1_项目骨架.md。

不要实现真实数据获取和模型训练，不要创建子Agent。
~~~

## 预期输出

- 完整项目目录。
- requirements.txt、配置和基础首页。
- Python 3.12 安装与启动说明。

## 测试要求

- python --version 显示 3.12.x。
- python -m pytest 能运行基础测试。
- app.py 和核心包可被导入。
- streamlit run app.py 可以启动首页。

## 验收标准

项目能够在干净的 Python 3.12 虚拟环境中安装并启动，不需要真实 Token。

## 下一阶段进入条件

配置测试通过，Streamlit 首页可启动，README 命令与实际一致。

---

# 阶段 2：Tushare、本地 CSV 与行情缓存

## 阶段目标

完成多数据源行情获取、标准化、质量检查和本地缓存。

## 前置条件

阶段 1 验收通过。

## 可复制提示词

~~~text
现在执行阶段 2：股票数据获取、本地CSV和行情缓存。

请完成：
1. 定义标准行情字段：ts_code、trade_date、open、high、low、close、vol、amount。
2. 实现Tushare客户端，Token从环境变量读取，支持股票代码和起止日期，对Token缺失、网络失败、空数据和接口异常给出清晰错误。
3. 实现本地CSV导入，支持字段校验、日期解析、数值转换、升序排序和错误报告。
4. 实现本地行情仓库，优先保存Parquet；依赖或格式不满足时允许回退CSV。
5. 支持保存、读取、覆盖更新和安全的增量更新。
6. 股票代码转换为安全文件名，防止路径穿越。
7. 生成数据质量报告：总行数、起止日期、重复日期、缺失值、非正价格、负成交量和OHLC逻辑异常。
8. OHLC检查包括high不低于open、close、low，low不高于open、close、high。
9. 创建scripts/fetch_data.py命令行工具。
10. 测试使用mock或固定样例，不访问真实Tushare。
11. 覆盖正常CSV、缺字段、日期乱序、重复数据、非法OHLC、缓存读写、Token缺失和空数据测试。
12. 更新README和数据字典，创建docs/progress/阶段 2_数据模块.md。
13. 运行本阶段测试和全部已有测试。

无Token时必须仍可通过CSV完成后续流程。不要创建子Agent，不要进入数据预处理阶段。
~~~

## 预期输出

- 统一行情 DataFrame。
- Tushare 客户端、CSV 导入器、本地仓库和质量报告。
- 命令行数据获取工具。

## 测试要求

测试不得访问互联网，至少覆盖正常、空数据、字段错误、日期错误和缓存往返一致性。

## 验收标准

无 Token 时能导入本地 CSV；有 Token 时接口具备完整错误处理；缓存读取结果与写入数据一致。

## 下一阶段进入条件

行情字段、类型和升序规则固定，数据质量测试全部通过。

---

# 阶段 3：数据清洗、时间划分与滑动窗口

## 阶段目标

建立严格无数据泄漏的数据预处理和 PyTorch 数据集流程。

## 前置条件

阶段 2 验收通过，标准行情格式稳定。

## 可复制提示词

~~~text
现在执行阶段 3：数据清洗、时间划分、归一化和滑动窗口。

请完成：
1. 按交易日期升序排序并删除重复日期。
2. 将行情字段转换为正确数值类型。
3. 缺失值默认只使用前向填充，不使用后向填充；开头仍缺失的数据应删除并记录。
4. 按70%、15%、15%时间顺序划分训练集、验证集和测试集，禁止shuffle，并保存各集合日期边界。
5. 检查三组目标日期不重叠。
6. 实现Scaler管理器，只允许训练集fit，验证集和测试集只能transform。
7. 保存特征列顺序、目标列、缩放参数和训练区间，支持安全加载与反归一化。
8. 实现滑动窗口，X形状为样本数、窗口长度、特征数，y为窗口后一个交易日close，同时保留目标日期和上一交易日真实close。
9. 支持单变量close和多变量open、high、low、close、vol、amount。
10. 验证集和测试集的首个输入窗口可以使用前一区间末尾历史数据，但目标日期必须属于当前区间。
11. 实现PyTorch Dataset和DataLoader辅助函数。
12. 对样本不足、比例错误、列缺失、非有限值和特征顺序变化给出清晰错误。
13. 测试时间排序、划分比例、无日期重叠、Scaler仅训练集拟合、窗口日期对齐、单变量/多变量形状和反归一化。
14. 创建docs/数据处理说明.md，使用具体日期示例解释窗口和跨边界上下文。
15. 创建docs/progress/阶段 3_数据预处理.md并运行全部测试。

本阶段的首要目标是防止未来数据泄漏。不要创建子Agent。
~~~

## 预期输出

清洗器、时间划分器、Scaler 管理器、窗口构造器和 PyTorch Dataset。

## 测试要求

必须包含会在整体数据拟合 Scaler 时失败的防泄漏测试，以及输入日期和目标日期的人工可验证测试。

## 验收标准

任何验证集或测试集数据都不能影响训练缩放参数；所有样本目标日期严格晚于其输入窗口。

## 下一阶段进入条件

防泄漏测试、日期对齐测试和反归一化测试全部通过。

---

# 阶段 4：Naive、MA、SES 与 ARIMA 基线模型

## 阶段目标

建立统一、可比较且不读取未来数据的传统模型基线。

## 前置条件

阶段 3 验收通过。

## 可复制提示词

~~~text
现在执行阶段 4：实现传统基线模型。

请完成：
1. 设计统一ForecastModel接口，至少包含fit、predict、get_params和model_name。
2. 统一预测结果字段：target_date、previous_close、actual、predicted、model_name。
3. 实现Naive：目标日预测等于目标日前一交易日真实收盘价。
4. 实现MA5、MA10、MA20：每个目标日只使用目标日前可见历史值。
5. 实现简单指数平滑SES，仅使用训练数据拟合，支持测试区间一步或预先定义的递推预测方式。
6. 实现ARIMA，使用有限的默认参数或配置参数，不进行无限范围搜索；拟合失败时返回明确错误。
7. 明确静态多步预测和滚动一步预测的差异，正式比较使用统一回测协议。
8. 对数据太少、非有限值、模型未拟合和日期不连续场景进行处理。
9. 测试Naive上一日逻辑、移动平均窗口、无未来数据、SES预测、ARIMA预测、异常处理和日期对齐。
10. 创建docs/基线模型说明.md和docs/progress/阶段 4_基线模型.md。
11. 运行本阶段测试和全部已有测试。

不要实现LSTM，不要创建子Agent。
~~~

## 预期输出

六种基线模型及统一预测结果结构。

## 测试要求

使用可手算的小序列验证 Naive 和移动平均；使用小型稳定序列验证 SES 和 ARIMA。

## 验收标准

所有基线模型输出相同字段和目标日期，且不会读取目标日或目标日之后真实值。

## 下一阶段进入条件

基线模型测试和未来数据泄漏测试全部通过。

---

# 阶段 5：PyTorch LSTM 训练、预测与持久化

## 阶段目标

实现可重复训练、可早停、可保存加载的单变量和多变量 LSTM。

## 前置条件

阶段 4 验收通过；阶段 3 数据接口保持稳定。

## 可复制提示词

~~~text
现在执行阶段 5：PyTorch LSTM模型、训练器、预测器和持久化。

请完成：
1. 实现batch_first=True的LSTM回归模型，input_size、hidden_size、num_layers和dropout可配置，输出下一交易日一个收盘价。
2. 单变量和多变量使用同一模型类，仅通过input_size和特征配置区分。
3. 实现随机种子工具，设置Python、NumPy、PyTorch和可用CUDA种子。
4. 实现训练器，使用MSELoss和Adam，记录训练损失、验证损失、epoch和耗时。
5. 使用验证集Early Stopping，保存验证损失最低的模型，不保存最后epoch冒充最优模型。
6. 自动选择CUDA或CPU；无GPU时必须正常运行。
7. 模型文件保存state_dict、模型结构参数、特征列、目标列、窗口长度、随机种子、最优epoch和验证损失。
8. 单独保存Scaler；加载时校验特征数量、特征顺序、窗口长度和模型参数。
9. 实现批量预测和下一交易日预测，支持目标close反归一化并保留日期。
10. 创建scripts/train_model.py，可从本地行情文件训练单变量或多变量模型。
11. 测试单变量和多变量前向传播、输出形状、小样本训练、Early Stopping、保存加载一致性、CPU运行和固定种子可复现性。
12. 测试不得使用测试集决定Early Stopping。
13. 创建docs/LSTM模型说明.md和docs/progress/阶段 5_LSTM模型.md。
14. 运行本阶段测试和全部已有测试。

使用小型合成序列做自动测试，不将其作为正式实验结果。不要创建子Agent。
~~~

## 预期输出

LSTM 模型、训练器、预测器、模型管理器和训练脚本。

## 测试要求

模型保存前后对同一输入的预测应在允许误差内一致；CPU 小样本训练必须完成。

## 验收标准

单变量和多变量模型都可训练、保存、加载和预测，测试集不参与早停。

## 下一阶段进入条件

模型生命周期测试、反归一化测试和可复现性测试通过。

---

# 阶段 6：评价指标、回测与实验管理

## 阶段目标

统一基线模型和 LSTM 的测试集回测、评价、结果保存和实验追溯。

## 前置条件

阶段 4 和阶段 5 均验收通过。

## 可复制提示词

~~~text
现在执行阶段 6：评价指标、回测服务和实验管理。

请完成：
1. 实现RMSE、MAE、R²、方向准确率DA和相对Naive RMSE提升率。
2. DA中预测方向比较predicted与previous_close，实际方向比较actual与previous_close；平盘按三分类符号一致规则处理并写入文档。
3. 相对提升率定义为(naive_rmse-model_rmse)/naive_rmse*100%。
4. 指标输入必须检查长度、日期、NaN、Infinity和最少样本数。
5. 实现统一测试集回测服务，使基线模型和LSTM在相同target_date集合上比较。
6. 验证集只用于选参数和早停，测试集只运行固定模型的最终评价。
7. 实现实验记录，保存experiment_id、时间、股票代码、数据区间、集合边界、特征、窗口、模型参数、随机种子、指标、状态和产物路径。
8. 保存实验配置JSON、指标JSON/CSV、逐日预测CSV和模型对比CSV。
9. 创建scripts/run_experiment.py，一次可运行Naive、MA5、MA10、MA20、SES、ARIMA和选定LSTM。
10. 对预测日期集合不一致的模型，先取明确记录的公共目标日期，不允许直接比较不同样本区间的指标。
11. 测试手算指标、DA上涨/下跌/平盘、提升率正负值、日期错位、NaN、实验保存读取和测试集隔离。
12. 用固定小型样例跑通端到端流程，并明确标记为测试结果。
13. 创建docs/评价与回测说明.md和docs/progress/阶段 6_评价回测.md。
14. 运行全部测试。

不要伪造正式实验结果，不要创建子Agent。
~~~

## 预期输出

统一指标、回测服务、实验服务、命令行实验工具和完整产物结构。

## 测试要求

必须手工验证指标公式，并验证所有模型使用相同目标日期和相同真实值。

## 验收标准

一次测试实验可以从行情数据生成模型对比表、逐日预测和实验配置，结果可完整追溯。

## 下一阶段进入条件

端到端测试通过，专项检查未发现测试集调参或日期错位问题。

---

# 阶段 7：Streamlit 可视化系统

## 阶段目标

将已有服务集成为适合普通用户和毕业答辩使用的交互系统。

## 前置条件

阶段 6 验收通过，业务服务接口稳定。

## 可复制提示词

~~~text
现在执行阶段 7：Streamlit可视化系统。

页面只能调用service层，不得复制核心数据处理、训练或评价算法。

请实现：
1. 首页：项目介绍、技术路线、使用步骤、当前状态和投资风险声明。
2. 数据获取页：股票代码、起止日期、Tushare获取、CSV上传、本地缓存、数据预览和质量报告。
3. 数据分析页：K线图、收盘价、成交量、MA5/10/20、收益率分布和相关性热力图。
4. 模型训练页：单/多变量、窗口、隐藏单元、层数、Dropout、Batch Size、学习率、Epoch、Patience和随机种子配置。
5. 训练必须由按钮主动触发，显示进度、训练/验证损失、最优epoch和耗时，页面刷新不得自动重复训练。
6. 预测分析页：模型选择、测试集真实/预测曲线、误差图、指标卡片和下一交易日预测。
7. 模型对比页：Naive、MA、SES、ARIMA、单变量LSTM和多变量LSTM指标表及RMSE、MAE、DA、相对提升率图。
8. 提供实验结果和逐日预测下载。
9. 使用st.session_state保存用户当前上下文，合理使用cache_data和cache_resource。
10. 对Token缺失、数据不足、模型不存在、配置不匹配和训练失败显示中文友好错误，不直接显示大段堆栈。
11. 所有图表使用Plotly，标题、坐标和帮助信息使用中文。
12. 支持加载预训练模型和本地缓存进行离线演示。
13. 添加service层和关键页面辅助逻辑测试，执行Streamlit启动冒烟检查。
14. 更新README和系统使用说明，创建docs/progress/阶段 7_Web系统.md。
15. 运行全部测试。

不要创建子Agent，不要在本阶段运行正式大规模实验。
~~~

## 预期输出

六个可用页面和完整用户操作闭环。

## 测试要求

验证无 Token CSV 流程、错误提示、模型加载、测试预测和结果下载；检查刷新不会自动训练。

## 验收标准

普通用户无需修改代码即可完成数据选择、模型训练、预测查看和模型比较。

## 下一阶段进入条件

Streamlit 可启动，核心页面流程可用，离线演示路径已验证。

---

# 阶段 8：正式实验与论文图表导出

## 阶段目标

使用真实历史行情运行可复现实验，形成论文所需表格、图表和客观分析。

## 前置条件

阶段 7 验收通过；正式行情数据和实验配置已准备。

## 可复制提示词

~~~text
现在执行阶段 8：正式实验和论文结果导出。

严格禁止伪造数据、指标和实验结论。

请完成：
1. 根据实际可用数据选取至少3只、建议5只不同行业股票，记录代码、行业、起止日期和样本数。
2. 所有股票使用统一的数据清洗、时间划分和评价协议。
3. 比较Naive、MA5、MA10、MA20、SES、ARIMA、单变量LSTM和多变量LSTM。
4. LSTM至少运行3个固定且不同的随机种子，报告均值和标准差。
5. 参数实验采用控制变量，不进行无边界搜索：窗口20/30/60，隐藏单元32/64/128，层数1/2，学习率0.01/0.001/0.0001。
6. 参数只根据验证集选择，选定后锁定并在测试集评价一次。
7. 完成单变量与多变量特征消融实验。
8. 保存每次实验的配置、模型、Scaler、训练历史、预测、指标、耗时和随机种子。
9. 导出正式实验汇总、模型指标汇总、参数实验汇总和特征消融实验CSV。
10. 导出真实值与预测值、模型RMSE、模型DA、训练损失、特征消融和窗口对比图。
11. 创建docs/实验结果分析.md，客观说明LSTM是否优于Naive、多变量是否稳定有效、不同股票差异和研究局限。
12. 如果正式数据不足，只生成实验配置和运行脚本并报告阻塞原因，不能使用合成数据冒充正式结果。
13. 检查所有论文表格都能追溯到实验ID和原始预测文件。
14. 创建docs/progress/阶段 8_正式实验.md并运行结果一致性检查。

不要创建子Agent，不要为了获得好看结果修改测试集或评价规则。
~~~

## 预期输出

真实实验数据、汇总表、论文图表和结果分析。

## 测试要求

检查实验 ID 可追溯、模型目标日期一致、CSV 与 JSON 指标一致、重复运行配置一致。

## 验收标准

所有结论都有真实实验产物支持；不存在测试集调参、选择性隐瞒或合成数据冒充。

## 下一阶段进入条件

正式实验汇总和论文图表完整，结果分析与实际指标一致。

---

# 阶段 9：全面测试、数据泄漏审查与答辩交付

## 阶段目标

以毕业答辩交付标准完成最终质量检查、离线演示和文档整理。

## 前置条件

阶段 8 验收通过，或正式实验的外部阻塞已如实记录。

## 可复制提示词

~~~text
现在执行阶段 9：全面测试、数据泄漏审查和答辩交付。

请完成：
1. 运行全部单元测试、集成测试和端到端测试。
2. 端到端流程必须覆盖：读取小型CSV、清洗、划分、缩放、窗口构造、小型LSTM训练、预测、指标和实验保存。
3. 专项检查训练/验证/测试日期重叠、整体数据fit Scaler、测试集调参、窗口日期错位、目标日信息泄漏和反归一化错误。
4. 检查Naive和移动平均未读取目标日真实值。
5. 检查所有模型在相同目标日期上比较。
6. 检查最佳验证模型而非最后epoch模型被保存。
7. 检查模型加载时验证特征列、输入维度和窗口长度。
8. 检查Token、密码、用户路径、固定盘符和敏感日志。
9. 测试空数据、缺字段、样本不足、Token缺失、无网络、非法股票代码、模型不存在、模型不匹配和ARIMA拟合失败。
10. 检查Streamlit不会刷新即训练，耗时操作有状态提示。
11. 完善README、安装说明、系统使用说明、系统测试报告、常见问题和答辩演示流程。
12. 准备离线答辩资源：本地样例行情、预训练模型、Scaler和已有实验结果；答辩现场不依赖网络和长时间训练。
13. 设计5至8分钟答辩演示：系统介绍、数据分析、加载模型、预测结果、模型对比、实验结论和局限。
14. 生成最终交付清单和已知问题清单。
15. 创建docs/progress/阶段 9_最终交付.md。
16. 不进行与正确性无关的大规模重构，不执行git commit。

完成后给出最终验收结论：通过、有条件通过或不通过，并列出答辩前必须解决的问题。不要创建子Agent。
~~~

## 预期输出

完整测试报告、安装和使用文档、离线答辩资源、演示流程和交付清单。

## 测试要求

全部自动测试通过；无法运行的测试必须记录原因；离线演示流程至少完整验证一次。

## 验收标准

系统可在 Python 3.12 环境安装和启动，可使用本地数据完成完整演示，实验结果可追溯且无已知严重数据泄漏。

## 下一阶段进入条件

本阶段为最终阶段。结论为“通过”或所有“有条件通过”的必须项完成后，项目进入论文定稿和答辩状态。

---

## 五、每阶段统一收尾提示词

每阶段开发任务完成后，必须向同一个 Agent 追加以下提示词：

~~~text
请结束当前阶段，不增加新需求。

依次完成：
1. 检查当前全部变更，确认没有误删或覆盖用户已有文件。
2. 运行本阶段相关测试，并在可行时运行全部pytest。
3. 检查是否包含硬编码Token、密码、绝对用户路径或固定盘符。
4. 检查本阶段是否可能引入时间序列数据泄漏。
5. 更新docs/开发任务清单.md。
6. 更新对应的docs/progress阶段记录，写明阶段目标、实际完成、修改文件、接口变化、测试命令、测试结果、遗留问题和下一阶段条件。
7. 最终汇报新增文件、修改文件、实现功能、测试结果、未完成事项和已知风险。
8. 如果验收未通过，留在当前阶段修复，不进入下一阶段。
9. 不执行git commit，不创建子Agent。
~~~

---

## 六、最终验收检查表

### 单 Agent 与环境

- [ ] 全过程仅由一个 Agent 串行执行。
- [ ] 未创建、调用或委派子 Agent。
- [ ] 所有环境说明和依赖均面向 Python 3.12。
- [ ] requirements.txt 可在 Python 3.12 环境安装。

### 数据正确性

- [ ] 行情按交易日期升序排列。
- [ ] 数据集按时间顺序划分且无目标日期重叠。
- [ ] MinMaxScaler 只在训练集拟合。
- [ ] 验证集用于调参和早停。
- [ ] 测试集仅用于最终评价。
- [ ] 滑动窗口没有使用目标日或未来数据。

### 模型与评价

- [ ] 实现 Naive、MA5、MA10、MA20、SES、ARIMA。
- [ ] 实现单变量和多变量 LSTM。
- [ ] 模型保存加载包含必要元数据校验。
- [ ] 实现 RMSE、MAE、R²、DA 和相对提升率。
- [ ] 所有模型使用相同目标日期比较。
- [ ] 正式实验结果真实且可追溯。

### 系统与交付

- [ ] 无 Token 时可通过本地 CSV 使用系统。
- [ ] Streamlit 页面不重复实现核心算法。
- [ ] 页面刷新不会自动触发训练。
- [ ] 系统包含明确的投资风险声明。
- [ ] 支持本地行情和预训练模型离线答辩。
- [ ] README、使用说明、测试报告和答辩流程完整。
- [ ] 未硬编码 Token、密码、用户路径或固定盘符。

---

## 七、建议执行顺序

同一个 Agent 每次只执行一个阶段：

1. 发送“单 Agent 通用提示词 + 阶段 0 提示词”。
2. 阶段完成后发送“统一收尾提示词”。
3. 验收通过后，再发送“单 Agent 通用提示词 + 下一阶段提示词”。
4. 从阶段 0 严格串行执行到阶段 9。
5. 任一阶段验收失败时，先修复当前阶段，不跨阶段绕过问题。

最终原则：**先保证数据处理、日期对齐和回测逻辑正确，再优化模型；先打通最小闭环，再完善页面；所有正式结论必须来自真实、可复现、无数据泄漏的实验。**
